sap.ui.define([
    "sap/ui/core/mvc/Controller"
], function (Controller) {
    "use strict";

    return Controller.extend("vendor.onboarding.controller.App", {
        onInit: function () {
            // App shell controller — routing is handled by the router
        }
    });
});
